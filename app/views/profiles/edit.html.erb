<% provide(:title, t('.title')) %>

<div class="block md:flex p-4 space-y-4 md:space-y-0 md:space-x-4">

  <%= render 'shared/settings_sidebar' %>

  <div class="md:basis-3/4 p-4 border border-gray-200 rounded shadow">
    <div class="text-left pl-4 pb-2 border-b border-gray-800 mb-2">
      <%= content_tag(:h1, yield(:title), class: 'text-xl') %>
    </div>
    <div class="text-sm">
      <%= render 'edit_form' %>
    </div>
  </div>
</div>
