<body class="body--signup"></body>

<div class="user__form">
  <!-- form -->
  <div class="nes-container is-rounded">
    <h1 class="title"><%= t('new_user') %></h1>
    <%= form_with(model: @user) do |f| %>
      <%= render 'shared/error_messages', object: @user %>

      <%= render 'form', object: f %>

      <div class="user__form-field">
        <%= f.submit t('signup'), class: 'nes-btn is-primary' %>
      </div>
    <% end %>
  </div>

  <!-- links -->
  <div class="user__form-links">
    <ul class="nes-list is-circle">
      <li><%= link_to t('login_user'), new_user_session_path %></li>
    </ul>
  </div>
</div>


