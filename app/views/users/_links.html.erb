<ul>
  <% if params['action'] == 'edit' || params['action'] == 'update' %>
    <li><%= link_to 'My page', user_path(@user) %></li>
    <li><%= user_destroy_link(@user) %></li>
  <% end %>

  <% if params['action'] == 'show' %>
    <li><%= link_to 'Edit', edit_user_path(@user) %></li>
  <% end %>
</ul>

