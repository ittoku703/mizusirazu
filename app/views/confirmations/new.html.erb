<body class="body--confirmation"></body>

<div class="form">
  <!-- form -->
  <div class="nes-container is-rounded">
    <h1 class="title"><%= t('activate_user') %></h1>
      <%= form_with(scope: resource, url: user_confirmation_path, method: :post) do |f| %>
        <%= render 'shared/error_messages', object: resource %>

        <%= render 'form', f: f %>
     <% end %>
  </div>
  <%= render "shared/links" %>
</div>

