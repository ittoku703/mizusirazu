$('#comments').append("<%= j render(
  'comments/comment',
  comment: @comment,
  comment_counter: -1)
  %>")

comment_id = '#comment-<%= @comment.id %> '
$(comment_id).find('#edit_comment_btn').on('click', () => {
  $(comment_id).find('#edit_comment_form').toggleClass('hidden');
});
