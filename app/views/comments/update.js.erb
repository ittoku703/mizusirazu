comment_id = '#comment-<%= @comment.id %>'
$(`${comment_id} #comment-content`).html("<%= j simple_format(@comment.content) %>")
$(`${comment_id} #comment-updated_at`).html("<%= j "#{t('updated_at')}: #{l @comment.updated_at}" %>")
