var comment = $('#comment_<%= @comment.id %>');

comment.replaceWith("<%= j render(
  'comments/comment',
  comment: @comment,
  comment_counter: -1)
  %>")

comment.find('#edit_comment_btn').on('click', () => {
  comment.find('#edit_comment_form').toggleClass('hidden');
});
