<div id="comments">
  <% if @micropost.comments.any? %>
    <%= render @micropost.comments %>
  <% end %>
</div>

<div class="py-4">
  <% if user_signed_in? %>
    <div class="nes-container with-title">
      <h1 class="title"><%= t('new_comment') %></h1>
      <%= form_with(url: comments_path, scope: :comment, id: 'new_comment-form', class: 'inline-block', method: :post, local: false) do |f| %>
        <%= render 'comments/form', f: f %>
      <% end %>
    </div>
  <% end %>
</div>
