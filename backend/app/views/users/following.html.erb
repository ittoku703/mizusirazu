<% provide(:title, t('.title', user: @user.name)) %>

<%= content_tag(:h1, t('.title', user: @user.name), class: 'text-xl text-left mt-2 ml-4 text-gray-800') %>

<%= render partial: 'users/user', collection: @user.following %>
